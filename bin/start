#!/bin/bash

read -d '' crontabStr << EOF
0 8 * * * python /data/assassin/v2ex_sign.py >> /var/log/v2ex_sign.log
EOF

echo "$crontabStr" | crontab
exec supervisord -c /etc/supervisor/conf.d/supervisord.conf


